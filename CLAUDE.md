# Правила работы

## Обзор кода перед завершением

После значимых изменений кода (не мелких правок текста/промптов) — прогонять `/code-review` перед тем как считать задачу завершённой. Уровень по умолчанию — стандартный (medium); для рискованных/крупных изменений использовать `high`.
