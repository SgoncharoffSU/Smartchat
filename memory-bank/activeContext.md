# Active Context — Smartchat

## Текущее состояние (по состоянию на перенос на code-server)
- Проект задеплоен и работает (локально на Windows и на проде у клиента).
- Репозиторий: `https://github.com/SgoncharoffSU/Smartchat.git`, ветка `main`.
- Исходный код закоммичен и запушен:
  - `67051ff` feat: фиксация исходного кода Smartchat (25 файлов).
  - `c5b6593` chore: .gitattributes для единых LF-переводов строк.
- В Git **не попадают**: `.env` (секреты), `data/leads.jsonl` (ПД), `node_modules/`,
  `dist/`, мусорные скрипты исследования.

## Что в работе
- Подготовка переноса разработки с локального VS Code (Windows) на code-server
  на VPS, чтобы работать с планшета Huawei MatePad 11.
- Готовятся файловый контекст (memory-bank), правила Cline (.clinerules) и
  инструкция для нового сервера (HANDOFF.md).

## Известные особенности и ограничения (из README)
- Раздел 10.5 (пользователь замолчал) не реализован — нужна очередь отложенных сообщений.
- FAQ-перехват построен на ключевых словах; возможны ложные срабатывания.
- Реальный хэндофф в Telegram/CRM/live-чат ещё не подключён (сейчас это факт логирования лида).

## Ближайшие задачи (перенос)
1. Закончить memory-bank и правила Cline (.clinerules), HANDOFF.md.
2. Подготовить `scripts/setup-code-server.sh`.
3. Подготовить `scripts/deploy-beget.sh` (данные Beget ожидаются от владельца).
4. Составить чек-лист ручных действий.

## Важно при работе в code-server
- Работать через терминал code-server, node окружение — Linux.
- Секреты держать только в `.env` на сервере, не в Git.
- Перед изменениями проверять `git status`.