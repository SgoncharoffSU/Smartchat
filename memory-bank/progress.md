# Progress — Smartchat

## Что сделано
- Реализован полный детерминированный сценарий диалога (конечный автомат, разделы 9–10).
- Статичный FAQ, детекторы инъекций/удаления данных/запроса человека.
- Валидация email/телефона/URL.
- Логирование лидов в `data/leads.jsonl` после согласия на ПД + квалификация A/B/C.
- Интеграция Битрикс24 `crm.lead.add` (опционально).
- Подключение YandexGPT для свободных вопросов (опционально, mock-режим без ключа).
- Веб-виджет `public/widget.js` + тестовая страница `public/demo.html`.
- Исходный код закоммичен и запушен в `origin/main`.

## Что осталось / ближайшие задачи
- [x] Завершить документацию переноса: `.clinerules`, `HANDOFF.md`, память.
- [x] Подготовить и выполнить установку code-server на VPS (IP и порт — только на сервере, в Git не попадают).
- [ ] Подготовить `scripts/deploy-beget.sh` (после получения данных Beget).
- [ ] Заполнить `.env` реальными данными на сервере.
- [ ] Пробный деплой на Beget.

## Отложено (не блокирует перенос)
- Таймер/напоминания для раздела 10.5 (пользователь замолчал).
- Реальный хэндофф в Telegram/CRM/live-чат.
- Улучшение FAQ с морфологией вместо ключевых слов.

## Развернуто на VPS (13.08.2026)
- Сервер: Ubuntu 24.04.4 LTS, Node v20.20.2, code-server v4.132.0.
- code-server запущен как systemd-сервис (user root), порт задан в конфиге на сервере.
- Проект склонирован в рабочую директорию на VPS, выполнен `npm install` (90 пакетов, 0 уязвимостей) и `npm run build`.
- Доступ снаружи подтверждён (HTTP 302 на `/login`).
- IP, порты и пароли — только на сервере и в локальных файлах `_*` (игнорируются git), в репозиторий не попадают.

## Правила ведения
- Отмечать выполненные пункты в конце каждой итерации.
- Не удалять историю завершённых задач, только дополнять сверху.